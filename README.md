# cdosborne.com redesign in django
This repo uses docker to create and run the website that uses:
* django
* postgres
* nginx
* tileservergl

See `docker-django-nginx` for details on the initial setup. 